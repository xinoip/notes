# Proxmox

Virtual machine madness.

## QEMU/KVM Manager

`qm` is a CLI tool to do all kinds of stuff with QEMU/KVM virtual machines.

```sh
# Import a image file as a new disk.
qm importdisk <vm_id> <img_file> disk-name
```
